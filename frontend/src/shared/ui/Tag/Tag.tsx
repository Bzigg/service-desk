import cls from './Tag.module.scss';

type TProps = {
    bgColor?: string;
    borderColor?: string;
    textColor?: string;
};

export const Tag = ({ bgColor, borderColor, textColor }: TProps) => {
    return <div>Tag</div>;
};
